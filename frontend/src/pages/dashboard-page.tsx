import { PageShell } from "../components/common/page-shell";

export function DashboardPage() {
  return (
    <PageShell title="Dashboard" description="High-level analytics will be wired in Step 10 and Step 11.">
      <p className="text-sm text-slate-600">Frontend base is ready with routing and Tailwind.</p>
    </PageShell>
  );
}
